# 자기소개서·면접 경험 작성 엔진 통합 기록

## 가져온 원본

- 사용자가 제공한 기존 서비스: https://jobnkill-essay-platform.jungdaewoong.chatgpt.site
- 원본 프로젝트: `appgprj_6a83d4a59234819193e5ac2788815b3d`
- 원본 브랜치: `main`
- 원본 커밋: `c19addb127caf588b5c6570e8c1d44c6a542199b`
- 원본 파일: `lib/narrative-engine.mjs`
- 통합본 위치: `public/vendor/jobnkill/narrative-engine.mjs`
- SHA-256: `4a266482207608d6894d7762311b7662be7685b28afc9ed0143f2339578c72c4`

위 원본 파일을 바이트 단위로 그대로 복사했다. 코드 동등성은 회귀 테스트에서 SHA-256으로 확인한다. 기존 서비스 전체나 로그인·보관함·회원 데이터베이스를 복제한 것은 아니다. 원본은 사용자가 제공한 자체 서비스의 코드이며 제3자 재배포 라이선스를 별도로 부여하지 않는다.

## 실제 재사용 범위

`public/writing-engine.js`가 다음 원본 함수를 실제로 호출한다.

| 입력 | 원본 함수 | 사용 범위 |
| --- | --- | --- |
| 경험과 상황 | `composeSituationNarrative` | `situationEvidence.scene`에 사용자가 쓴 내용만 전달 |
| 내가 한 행동 | `composeActionNarrative` | `actionDetail`에 사용자가 쓴 내용만 전달 |
| 결과와 확인 근거 | `composeResultNarrative` | 입력한 결과의 문장 정리 |
| 배운 점 | `composeLearningNarrative` | 입력한 배운 점의 문장 정리 |

상황·행동 함수의 `allowGuidedFallback`은 `false`다. `composeContextNarrative`, 사고방식 추정, 선택 카드의 일반 업무 문장 생성은 사용하지 않는다. 별도 AI 호출·서버 호출·계정 접근·작성문 전송은 없다.

## 원문 보존 기준

원본 엔진에는 취업 컨설팅 맥락의 문장 재작성 규칙이 있다. 고교 활동 내용이 그 규칙에 의해 바뀌거나 추가되지 않도록 어댑터는 결과를 다음과 같이 검사한다.

1. 문장부호·공백 정리, 명시한 문장 끝 표현 변경, 배운 점 앞의 연결 표현만 허용한다.
2. 원문과 위 허용 표현으로 결과 전체를 재현할 수 없으면 해당 항목의 원문을 사용하고 경고를 반환한다.
3. 원문의 수치, 부정 표현, 세부 내용 삭제나 새로운 행동·생각·결과의 삽입을 추측해서 허용하지 않는다.
4. 원본 엔진의 내부 글자 수 제한 때문에 긴 입력이 잘리더라도 원문으로 복원한다.
5. 최종 문장 정리 후 전체 길이가 16,000자를 넘는 경우 전체 입력을 유지한다.
6. `originalText`와 항목별 `original`, `text`, `changed`, `preserved`를 반환하여 사용자가 원문과 정리한 문장을 비교할 수 있게 한다.

입력은 전체 20~16,000자이며, 필드 사이의 문단 구분을 포함해 센다. 잘못된 타입이나 범위를 벗어난 입력은 사용자에게 보여줄 메시지와 오류 코드를 반환하는 예외로 처리한다. 비어 있는 행동·결과·배운 점은 생성하지 않는다.

## 피드백 범위

`reviewWritingDraft`는 입력한 문장의 표현을 찾아 상황, 역할, 행동과 이유, 결과와 근거, 배운 점의 보완 질문을 반환한다. 인용 근거는 실제 입력한 문장에서만 가져온다. 학교·학과 이름은 사용자가 선택한 맥락으로만 사용하며 교육과정 사실을 자동 생성하지 않는다.

이 기능은 글의 구조를 점검하는 규칙 기반 도구다. 경험의 사실 여부, 표현의 의미적 정확성, 대학 심사 결과를 검증하는 전문 채점기가 아니다. 점수·합격확률을 반환하지 않는다. 대학 제출 여부를 가정하지 않으며 면접을 위한 경험 정리에도 사용할 수 있다.

## 검증

`node --test tests/writing-engine.test.mjs`의 8개 테스트가 통과했다. 원본 파일 동일성, 실제 문장 정리, 비어 있는 내용 보존, 취업용 재작성 규칙 차단, 16,000자 원문 보존, 입력 오류, 실제 문장 인용 피드백, 소수 수치와 원문 보존을 검사한다. 실제 사용자 검수 결과와 구분되는 내부 코드 검사다. 화면·저장·배포 검증은 해당 통합 검수 기록에서 별도로 관리한다.

{
  "name": "jobandkill-naver-blog-studio",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "description": "Source-first Naver blog drafting, review, and manual publishing assistant",
  "scripts": {
    "start": "node scripts/serve.mjs",
    "build": "node scripts/build-static.mjs",
    "check": "node --check core.mjs && node --check app.mjs && node --check scripts/serve.mjs && node --check scripts/build-static.mjs",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "UNLICENSED"
}

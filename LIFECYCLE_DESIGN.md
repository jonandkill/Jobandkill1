# 입시 준비 생애주기 UI 개선 기록

작성: 2026-09-10 · 담당: Team 5 생애주기 UX·디자인

## 문제와 결정

기존 홈은 모든 방문자에게 동일한 2.3등급 예시와 기능 카드 4개를 보여주었다. 사용자가 실제로 학교를 찾고, 성적을 정리하고, 후보를 줄이고, 반복 연습하고, 접수 전에 확인하는 서로 다른 시점을 구분하지 않았다. 화면을 열었다고 전형 준비가 완료되었다고 볼 수도 없다.

이번 변경은 **탐색 → 성적 정리 → 지원 후보 → 실전 연습 → 지원 점검**의 다섯 작업을 홈에서 바로 선택하도록 한다. 의무 순서나 합격 준비도 점수를 만들지 않는다. 모든 단계는 처음부터 접근 가능하다. 실제 입력 평균과 후보 수만 상태로 보여준다.

## GitHub와 공식 디자인 시스템 재검토

기존 `DESIGN_RESEARCH.md`의 30개 사이트 조사 기록을 읽었다. 그 기록은 공개 본문·경로 구조 확인과 내부 설계 가설이며, 30개 동영상의 재생 검사 또는 실제 사용자 투표가 아니다. 이번에는 기능 실패를 먼저 해결하기 위해 구현과 접근성 지침이 공개된 다음 두 저장소를 GitHub 커넥터로 직접 읽었다.

| 참고 | 실제 확인 | 이 서비스 적용 |
| --- | --- | --- |
| [GOV.UK Frontend](https://github.com/alphagov/govuk-frontend) | README의 서비스 UI·접근성 지침·라이선스 안내 | 작업별 짧은 이름, 상태 문구, 넓은 클릭 영역 |
| [US Web Design System](https://github.com/uswds/uswds) | README의 공개 컴포넌트·모바일·접근성 지원·패키지 구조 | 주요 단계와 내용 제목 구분, 좁은 화면에서 단계 정보 유지 |

[GOV.UK Task list](https://design-system.service.gov.uk/components/task-list/)는 여러 번 방문하면서 사용자가 작업 순서를 선택하는 업무에 맞는 패턴을 설명한다. 이번 홈에서는 이를 참고해 전체 행을 링크로 만들고, 상태를 클릭 버튼처럼 별도로 꾸미지 않았다. [USWDS Step indicator](https://designsystem.digital.gov/components/step-indicator/)의 진행 안내 패턴은 입력 단계와 홈의 탐색 단계를 혼동하지 않는 참고 자료로 검토했다. 홈의 다섯 작업은 입력 진행률이 아니며, 실제 입력 마법사 진행률과 별개다.

문서와 구조 원칙을 참고해 기존 앱 위에 자체 HTML/CSS를 작성했다. GitHub 저장소의 디자인 시스템 전체나 이미지·상표·로고·템플릿 코드를 복제하지 않았다. 이번 선정은 제품 요구와 구현 제약을 바탕으로 한 내부 설계 판단이다. **실제 사용자 투표나 우수성 실험을 수행했다고 주장하지 않는다.**

## 적용한 생애주기와 실제 경로

| 단계 | 사용자에게 보이는 행동 | 경로 | 표시 근거 |
| --- | --- | --- | --- |
| 탐색 | 지역·대학·학과 찾아보기 | `#find` | 부모 앱에서 전달한 활성 대학 수. 미전달 시 수치 없이 전국 대학 탐색 |
| 성적 정리 | 평균 입력·계산, 세부 성적 추가, 입결 비교 | `#recommend` | 입력값이 등급제 범위 내일 때만 실제 평균 표시 |
| 후보 비교 | 담은 학교 나란히 비교 | `#saved` | 부모 앱에서 전달한 후보 수 |
| 실전 연습 | 논술 또는 면접 연습 | `#prepare/essay`, `#prepare/interview` | 기능 진입. 연습 완료·숙련도 표시는 하지 않음 |
| 지원 점검 | 이번 주 준비·서류·일정 확인 | `#prepare/plan` | 기존 체크리스트 진입. 원서 접수 완료를 뜻하지 않음 |

홈 `renderHome(root, profile, onStart, context = {})`에 선택 인자를 추가했다. 기존 3개 인자 호출도 작동한다. `context.savedCount`와 `context.universityCount`는 부모 앱의 실제 상태를 전달받는다.

평균 입력 전에는 ‘평균 내신 입력하고 시작’이 우선 버튼이다. 이미 유효한 평균이 있으면 ‘내 성적으로 대학 살펴보기’로 바뀌고, 예시 카드도 내 입력값으로 표시한다. ‘성적 입력·평균 계산’과 ‘입결 비교·세부 성적 추가’를 홈에서 명확하게 노출한다.

## 정보 구조·가독성

- 다섯 작업을 반복 카드 대신 넓은 행으로 구성했다. 제목, 한 문장 설명, 실제 상태 또는 행동을 한 줄의 묶음으로 읽는다.
- 홈페이지는 밝은 크림색과 짙은 글자를 유지한다. 작업 내용보다 장식이 먼저 보이지 않도록 모션 영역 높이를 줄였다.
- 모션은 설명용 코드 그래픽이다. 영상 또는 실제 입결 그래프로 가장하지 않는다. 멈춤 버튼과 운영체제의 움직임 줄이기 설정을 유지한다.
- 논술·면접·자료 탐색은 실전 연습 영역에서 별도 경로로 바로 들어간다.
- 과거 입결이 실제 합격을 보장하지 않는다는 설명은 일반 본문 크기의 확인 문구로 보여준다.
- 학교명·전형명·긴 출처는 열 안에서 줄바꿈한다. 숫자표는 내용 구역 안에서만 가로 이동한다.

## 다른 팀과의 협의·반영

1. 성적·대학 팀: 반복 입결 카드마다 대학명이 필요하다는 사용자 문제를 전달했다. 해당 팀은 대학 카드 목록과 대학 선택 뒤 입결 비교로 나누고 제목에 대학명을 포함한다고 회신했다. `.grade-input`, `.grade-entry`, `.school-candidate`, `.school-result-summary`, `.recommend-detail`의 반응형 스타일을 지원했다.
2. 논술 표시 팀: 큰 빈 PDF 프레임이 가장 중요한 실패라고 전달했다. 해당 팀은 캔버스 기반 원문 읽기·상태·오류 복구·페이지 이동을 구현한다고 회신했다. `.document-reader`, `.practice-timer`, `.practice-question-body`, `.practice-answer-area`, `.practice-feedback`를 지원했다.
3. 면접 팀: 나뉜 다섯 입력 칸 대신 단일 답변과 인용 근거 피드백을 지원했다. `#iv-answer`, `#iv-question-text`, `#iv-feedback`, `#iv-revise`의 읽기·수정 동선을 스타일로 보완했다.
4. PM: 홈에 실제 대학 수·후보 수 전달이 필요함을 공유했다. 앱·서버·라우터는 PM의 통합 책임이며 이 팀은 해당 파일을 수정하지 않았다.
5. 검수 팀: 320·390px 홈, 입력 뒤 홈 복귀, 전체 행 버튼, 입력 중 후보함 가림을 실제 브라우저에서 확인하도록 전달했다.

## 모바일과 접근성

데스크톱 후보함은 본문 옆 sticky 영역을 유지한다. 1100px 이하에서는 접이식 후보함과 하단 메뉴를 유지하되, 본문 입력·선택·답변 요소에 포커스가 있으면 후보함을 숨겨 작성 공간을 확보한다. 입력이 끝나면 후보함이 다시 표시된다. `:has()`를 지원하지 않는 구형 브라우저는 기존 도크 위치로 작동하므로 실기기 검수가 필요하다.

홈 단계는 760px 이하에서 번호·내용·상태의 두 열로 재배치된다. 폼 필드는 모바일 한 열이며, PDF 캔버스는 컨테이너 폭 안에 표시한다. 타이머 숫자는 고정폭 숫자형으로 흔들림을 줄인다. 논술·면접 답변은 넓은 단일 편집면으로 표시한다. 기능 버튼과 후보함 열기는 최소 44px 기준을 적용한다.

## 검증과 남은 범위

- 수행: 변경한 홈 모듈의 `node --check`, `git diff --check` 통과.
- 수행: 실제 경로가 기존 해시 라우터와 일치하는지 소스 확인, 동적 값 HTML 이스케이프, 입력되지 않은 등급을 완료로 표시하지 않는 분기 확인.
- 인계: 브라우저 320·390·768·1440px 레이아웃, 대학 선택→입력→연습 동선은 통합 후 검수 팀/PM이 실행한다. 이 문서만으로 실행 완료라고 보고하지 않는다.
- 미수행: 실제 수험생·학부모 모집 검사, Android/iPhone 실기기 키보드·보조기술 검사, 전환율·이탈률 실험. 내부 검사와 실제 사용자 검수를 구분한다.

유지 기준: 새로운 기능은 해당 생애주기 단계 하나에 먼저 연결하고, 다음 행동과 돌아갈 경로를 제공한다. 자료가 없는 상태는 빈 영역 대신 확인된 범위와 다시 시도·다른 자료 선택을 보여준다. 외관 변경만으로 기능 실패를 완료 처리하지 않는다.

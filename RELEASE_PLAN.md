# 1.3 실제 작성 파일럿 — 출시 보류 조건 포함

## 문제와 수용 기준

기존 createKeywordDraft는 5개 분야 문단의 고정 조합이었다. 일반 정보로 분류되는 '야쿠르트 몸에 좋은 이유'에서도 검색 목적, 비교 기준 같은 글쓰기 안내가 출력된다. 이는 HTTP 장애가 아닌 핵심 기능의 의미적 실패다. 기존 75점은 사실성 또는 관련성 평가가 아니다.

수용 기준: 특정 키워드의 독자 질문에 답하는 본문, 선택형 이미지(기본 1장), 입력 보존, 명확한 실패 메시지, 임의의 치료·가격·일정·후기·논문 생성 금지, 100회 API 예산 제한. API 연결 전 일반 템플릿을 성공 결과로 표시하지 않는다.

## 역할과 실행 순서

| 단계 | 담당 | 산출물/출시 기준 |
|---|---|---|
| 기획 | PM | 실제 본문과 글쓰기 안내 구분, 예산·시크릿 경계 |
| 조사 | 벤치마킹팀 | 공식 문서로 모델 가격·조사형 작성 구조 비교 |
| 파일럿 | 서버팀·화면팀 | 인증된 동일 출처 API, 이미지 옵션, 오류 회복 |
| 실행/최적안 | 통합팀 | 작은 단일 서버, 제한된 토큰, 자동 재시도 없음 |
| 가상 사용자 100건 | QA·CS팀 | mock 응답 기반 계약·실패·예산 검사; 실사용자 아님 |
| CS 반영 | 개발팀 | 본문 보존, 비용 차단, 잘린 응답과 무관한 본문 거부 |
| 운영 검증 | 운영팀 | 실제 키로 글·이미지, 사용량·지연·품질 검수 |
| 출시 | PM | 실제 호출·모바일 검수·영속 예산장부 확인 후에만 |

## 조사와 선택

- STORM: 자료 조사와 목차·본문 작성을 분리하는 구조를 참고한다. https://github.com/stanford-oval/storm
- GPT Researcher: 조사 출처 추적과 보고서 작성 분리, 이미지 선택 흐름을 참고한다. https://github.com/assafelovic/gpt-researcher
- Dify: 검색 시험과 운영 검색 결과를 비교하는 검증 흐름을 참고한다. https://docs.dify.ai/en/cloud/use-dify/knowledge/test-retrieval

오픈소스 코드를 복사하거나 설치하지 않고 구조만 참고한다. 전체 자율조사 프레임워크는 반복 검색과 추론 비용이 있어 이번 저비용 파일럿에서는 채택하지 않는다. 검증 자료 입력 + 제한된 생성 호출을 사용한다. 현재 파일럿은 실시간 검색/RAG나 논문 검증을 구현한 것이 아니며 건강·법률·금융 글의 검증된 출처 확보는 별도 출시 기준이다.

## 비용 산정 근거 (2026-09-10 조사)

GPT-4.1 mini 입력/출력 100만 토큰당 $0.40/$1.60. GPT Image1 mini low 1024 정사각형 출력 $0.005 + 텍스트 입력 100만 토큰당 $2.

https://developers.openai.com/api/docs/models/gpt-4.1-mini
https://developers.openai.com/api/docs/models/gpt-image-1-mini

예시 가정: 글당 입력 2,500+출력 2,500 토큰은 $0.005. 이미지 프롬프트 500토큰+저품질 1장은 $0.006. 100건 $1.10 × 예산용 환율 1,800원 × 여유계수 1.15 = 2,277원. 실측 청구액이 아니며 웹검색·서버·스토리지 요금은 제외한다. 이미지 고품질/여러 장은 같은 예산으로 보장하지 않는다. 구현에서는 정확한 토크나이저 대신 요청 전체 UTF-8 바이트 수를 보수적 상한으로 잡는다. 입력 상한 8,000, 출력 2,500, 이미지 프롬프트 상한 800 기준 1건 최대 약 28.57원, 100건 2,857원 예약이다. 실패도 지출 가능성이 있으므로 예약을 자동 반환하지 않는다. 한도에 도달하면 중단하며 100건 성공을 보장하지 않는다.

Image1 mini는 종료 예정 모델이다. 2026-12-01 종료 공지: https://developers.openai.com/api/docs/deprecations . 후속 모델로 무단 전환하지 않고 가격 재검증이 필요하다.

## 운영과 배포 경계

현재 Render 대상은 static_site다. 정적 번들에 API 키를 넣어도 서버가 생기지 않으며 키 유출 위험이 있다. 이번 수정본은 별도 파일럿 브랜치에 보존하고 기존 main 공개 사이트를 서버 없는 새 UI로 교체하지 않는다.

실제 가동에는 서버 전용 OPENAI_API_KEY, STUDIO_ACCESS_TOKEN(24자 이상), 영속 파일 경로 STUDIO_LEDGER_PATH가 필요하다. 호스팅에서는 HOST=0.0.0.0을 설정하고 PORT는 호스팅에서 제공하는 값을 사용한다. 비밀값은 환경변수로만 전달하고 저장소·브라우저 로컬 저장소·채팅에 기입하지 않는다. 서버는 단일 인스턴스 전용이다. 컨테이너 임시 저장소만으로 예산장부를 유지하면 재배포 때 한도가 초기화되므로 소비자 공개 금지. 유료 서버/디스크 생성은 별도 승인을 받아야 한다.

## 측정과 과장 금지

- 가동률: 실제 관측 기간의 성공 요청/유효 요청. 아직 운영 수치 없음.
- 품질: 주제 질문 답변 여부, 출처-주장 일치, 오류·중복, 편집 필요량. mock 통과율로 품질을 주장하지 않는다.
- 생산성: 초안 완성 시간과 수정 시간, 글당 실제 토큰 비용. 개선율은 전후 실측 후에만 발표한다.
- 소비자 CS: 100개 합성 시나리오는 실제 고객 피드백·만족도·부하 테스트를 대체하지 않는다.

API 연결/실제 샘플 검수/기기 검수/영속장부 준비 전 상태는 '파일럿 개발', '소비자 출시 완료'가 아니다.

## 이번 실행 결과

2026-09-10: 문법 검사, 전체 자동 검사 138건(그중 합성 시나리오 100건), 정적 빌드 통과. 실제 유료 API 호출은 0회. 검수용 브라우저의 로컬 주소 접근은 ERR_BLOCKED_BY_CLIENT로 차단되어 화면·모바일 검수 미완료. 실제 API 출력의 내용 정확성 및 이미지 품질은 미검증이다. 합성 100건 통과는 장애처리 계약 검사 결과이며 실제 가동률이나 소비자 만족도 수치가 아니다.

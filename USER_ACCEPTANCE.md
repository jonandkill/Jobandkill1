# 진학설계 사용자 검수 계획

실제 사용자 조사는 아직 시행하지 않았다. 아래는 모집 후 사용할 검수 시나리오다.

## 비교 방식

수험생과 학부모를 구분해 기존 입력 화면과 단계형 화면에서 동일한 과업을 수행하도록 한다. 제시 순서를 교차해 먼저 경험한 화면에 의한 학습 영향을 줄인다. 동의 없이 화면·성적·연락처를 수집하지 않는다. 실제 성적 대신 가상 사례를 기본으로 제공한다.

## 과업

1. 평균 내신만 아는 가상 수험생이 대학 탐색 결과까지 도달한다.
2. 평균 내신을 모르는 수험생이 건너뛰어 대학을 찾는다.
3. 마지막 확인에서 등급 체계가 잘못됐음을 발견하고 돌아가 수정한다.
4. 희망 지역을 정하지 않고 탐색한 뒤 지역을 바꾼다.
5. 대학의 지원자격·수능최저·고사일을 찾고 자료 기준연도를 말한다.
6. 지정 대학의 지정 연도 논술 기출을 찾고 모의논술과 구분한다.
7. 두 대학을 저장하고 비교한다.
8. 자료 미수집과 조건 불충족을 구분해 설명한다.

## 기록과 개선 판단

과업 완료 여부, 첫 결과까지 시간, 잘못 입력한 횟수, 도움 요청, 이탈 단계, 결과 의미의 이해 여부를 기록한다. 표본 규모와 모집 방식을 결과에 함께 남긴다. 소규모 정성 검수를 전체 이용자 전환율 향상의 근거로 일반화하지 않는다.

입력값 소실, 빈 화면, 잘못된 합격확률 표시, 모바일 주요 버튼 가림은 출시 차단 항목이다. 단순 선호 의견은 반복 빈도와 과업 실패 영향을 구분해 우선순위를 정한다.

## PM 재반영

VOC별로 문제 → 재현 절차 → 영향 → 담당 → 수정 → 재검수 기록을 남긴다. 웹에서 작성한 로컬 오류 메모는 실제 접수된 고객 요청으로 집계하지 않는다. 데이터 누락 신고는 출처 확인 후 반영하며 대학 자료와 다른 사용자 진술을 바로 공식 정보로 대체하지 않는다.
# 2026-09-10 추가 구현 및 내부 인수 검사

- 기본 평균 2.3등급 입력 → 4단계 완료 → `#recommend` 입결 비교 이동을 공개 사이트에서 확인했다.
- 국어·영어·수학·사회·과학 선택 입력과 저장을 구현했다. 현재 자료로는 교과 평균만으로 대학별 환산등급을 자동 계산하지 않는다.
- 과거 성적 표에 내 전체 평균을 별도 기준으로 병렬 표시한다. 직접 차이·분포 거리 계산은 검증된 같은 산식의 환산등급 입력에 한정한다. 학교 목록 순서는 희망 조건·자료 연수 기준이며 합격 순위가 아니다.
- 공개 사이트에서 대학 담기 후 우측 후보 1개 및 학교명 표시를 확인했다.
- 논술 학교·계열 선택, 답안 작성, 공식 항목 자기체크 10/40점, 분량 부족 안내가 표시됨을 확인했다. 이것은 자동 정답 채점이 아니다.
- 논술 페이지 `1 / 8`에서 `2 / 8`로 키보드 이동을 확인했다. 중앙 배치와 이전 버튼 비활성화를 확인했다.
- 면접 울산대 안내, 답변 1/5 작성, 키보드 제출 후 누락 항목·추가 질문 출력을 확인했다.
- 320px 홈, 390px 논술 자료, 768px 입결 비교, 1440px 홈의 프레임 내부 문서 가로 넘침이 없음을 검사했다. 스크린샷으로 홈 시각 구성을 확인했다. 실제 Android/iPhone 기기 검사는 아니다.
- 일부 긴 화면의 마우스 자동 클릭은 브라우저 제어 시간 초과가 발생했다. 같은 버튼의 키보드 실행은 성공했으며 실제 터치 성공으로 바꾸어 보고하지 않는다.
- 단위 테스트 20개 통과 및 기존 데이터 검사 통과. 기본 탐색·저장·연습 피드백은 AI API 호출 없이 동작한다.

## 조사와 데이터 범위

- 공식 디자인 참고 30개, 자체안 2개, 가상 요구조건 6개 가중합에서 A안 5/B안 1. 실제 사용자 0명·실제 투표 0건.
- 논술 공식 PDF 33개를 실제 내려받아 검토. 문항별 배점 행 209개, 공식 PDF 10개, 채점표 범위 35개. 209종의 일반 평가법이나 전문가 검증 완료가 아니다.
- 면접 안내는 12개 대학의 일부 전형. 기출 개별 게시물 5건·확보 본문 0건·검증 후기 0건. 자체 연습 5개를 기출로 표시하지 않는다.
- 전체 논술 대학의 모든 문항·학과별 안내·전형 상세는 여전히 미완비. PDF는 공식 서버의 파일 열기·뷰어 저장을 제공하며 전체 문항을 자체 서버에 복제한 상태가 아니다.
- 회원 동기화, 실제 후기 수집·운영자 승인, 전문가 채점, 의미·정답 판단, 신뢰할 만한 개인 합격 추천 순위는 미구현이다.

---

## 2026-09-10 v3 내부 통합 검사 (기존 검사와 구분)

학교 우선 카드/페이지, 최초 입력 선택 세부성적/평균 적용, 자체 문항 본문/기준답안, PDF.js 캔버스, 시험 타이머, 단일 면접 답변과 로컬 자기소개서 추출로 개선했다. 이전 자기체크 점수와 5개 분리 답변 칸은 현재 방식이 아니다.

자동 검사 57개 통과: 성적 평균·단위 누락·체계 불일치, 학교 페이지/별칭/특수법 목록, 입결 산식 제한, PDF 중계 허용목록, 종료시각 기반 타이머, 수치 정답의 한정 배점, 서술근거/키워드반복/모순, 면접 단일 답안·캠퍼스 분리·첨부 제한, 교육자료 원본 보존. 기존 데이터 검사도 통과했다.

로컬 서버 응답: 카탈로그 360개 항목(기존352+추가8, 확인폐교/통합3은 탐색에서 제외), 입결40,971행/183코드, 연습/표준/교육JSON 및 PDF.js 모듈200응답, 특수법대학 입결없음은 오류없이빈목록. 이 숫자는 전국 모든 전형 상세를 갖췄다는 의미가 아니다.

현재 셸 환경에서 대학 PDF 호스트의 DNS 조회가 제한되어 외부 PDF 중계 성공은 로컬에서 확인하지 못했다. 실제 Render 운영에서 캔버스 표시·입력·채점·면접 동선을 추가 검수한 뒤 결과를 아래에 기록한다. 실제 수험생/학부모 테스트 참여자 수는 0명이며 자동 검사/내부 브라우저 검사를 실제 사용자 조사로 표현하지 않는다.

### v3 공개 운영 화면 내부 검수

Render 배포 `dep-dah0mfajnfac73fpiirg`, GitHub 커밋 `6c9b97f`의 live 전환을 확인했다. GitHub API 업로드 후 자동 배포 작업이 생기지 않아 캐시 초기화 재배포를 직접 요청했다. 자동배포 연결 자체가 수리됐다고 주장하지 않는다.

- 최초 4단계에서 세부성적을 펼쳐 국어2.3·수학1.8을 입력: 단순평균2.05, 단위3·5를 넣으면 가중평균1.99. 평균 적용 후 최종확인에 '입력 과목 평균1.99' 표시, 입결 탐색 이동 성공.
- 울산지역 학교 카드3개가 먼저 보이고, 울산대 선택 후 학교명을 포함한 입결표5개/쪽, 전체7쪽 표시. 대학 상세의 원서·평가·최저·서류 안내 → 학과20개 → 연도별 입결 이동을 클릭 확인.
- 광운대2026 자연1교시 공식 PDF 17쪽을 캔버스에서 실제 시각 확인. 중앙 페이지 버튼1→2쪽 이동. 120분 시작 후119:49, 새로고침 후119:04로 이어지고 수치답602가 복원됨.
- 답602는 최종수치2/2점으로 표시되며 소문항전체10점 중 증명8점 미평가를 구분했다.
- 면접 울산대·간호학과 선택, 가상 활동문장 붙여넣기→근거문장3개 기반 질문→단일답변→보완할점·인용·후속질문 출력 확인.
- 320px 홈과390px 면접 프레임의 가로 넘침 없음. 390px(스크롤바 제외375px) 답변 포커스시 후보함숨김 확인. 실제휴대폰 키보드·터치검수는 아님.
- TXT 가상파일 첨부는 브라우저에서 읽기실패 안내가 나와 통과로 기록하지 않았다. 같은 파일의 File API 추출은 로컬에서145자 정상 확인. 검수브라우저 파일경로 접근과 실제 이용자 기기의 첨부 성공 여부는 미확정이며 PDF 첨부 추출도 실기기검수가 남았다. 붙여넣기 질문 생성은 성공했다.

검수 중 새학교목록이 이전5분캐시에 남는 현상을 발견해 카탈로그 재검증 요청을 적용했다. 저장된 세부성적이 추천화면에서 자동으로 길게 펼쳐지는 문제도 기본접기로 수정했다. 모바일 PDF 확대 기능과 면접 인용문 관련성 개선을 추가 반영했다.

### 최종 회귀 수정

공개 PDF 열람 뒤 자체 문항으로 바꿀 때 `PDFDocumentProxy.destroy()`가 없어 전환을 막는 오류를 실제 콘솔에서 확인했다. PDF.js6의 `PDFDocumentLoadingTask.destroy()`로 정리하며, 취소/정리 오류가 다음 문항을 막지 않도록 수정했다. 면접 PDF 첨부도 같은 잘못된 정리 함수를 고쳤다. 실제 PDF 바이트를 현재 PDF.js로 추출하고 해제하는 검사와 뷰어 자원정리 검사까지 포함해 전체64개 테스트가 통과했다.

면접 피드백은 상황·역할·행동·결과를 첫 문장 하나로 중복 표시하던 부분을 수정했다. 단순 '기록' 명사로 결과를 확인한 것으로 판정하지 않으며, 강한 범주 단서를 가진 문장을 우선 인용한다. 미수집 학과는 직접 입력해 자체 연습에 사용할 수 있고 공식 전형 선택과 구분한다. PDF는 화면맞춤·125/150/200% 확대를 지원하며 확대 영역 안에서만 가로 이동한다.

내부 프레임검사: 768px/1440px 홈도 문서 가로 넘침이 없었다. 스크롤바를 제외한 내부폭753/1425px와scrollWidth가 일치했다.

## 자기소개서 작성 내부 통합 인수 기준 — 미실시

기존 서비스 주소 및 원본 코드 구조를 확인했다. 다음 항목은 구현 후 실시할 검수이며 통과 실적으로 집계하지 않는다.

- 대학·학과를 선택한 뒤 사이트를 떠나지 않고 면접용 경험 정리를 시작하며 같은 정보를 재입력하지 않는다.
- 자유 작성 → 단계별 보완 → 작성문 확인 → 선택한 글로 면접 질문 생성 → 답변·피드백을 완료한다. 제출 서류 작성과 면접용 경험 정리의 차이를 이해할 수 있다.
- 학교·학과 변경, 이전/다음 이동, 질문 변경 시 초안이 유지된다. 기기 저장을 선택한 경우 새로고침 후 복원하며 선택하지 않은 글이 저장되었다고 안내하지 않는다.
- 작성문은 질문 생성 버튼을 누른 뒤 반영된다. 첨부·다른 초안·이전 질문의 답변을 섞지 않고, 자료 삭제 시 연결된 질문·답변의 처리 범위를 안내한다.
- 모바일 키보드가 열린 상태에서 입력·단계 이동·저장 버튼을 사용할 수 있으며 후보함이 가리지 않는다. 저장 실패·로그인 만료·연결 실패 시 현재 작성문을 보존한다.
- 기존 자기소개서 회원 프로젝트 연동이 구현되면 사용자별 접근 격리, 계정 연결 해제, 버전 충돌과 복원도 별도로 검사한다. 현재는 연동이 없는 상태다.

실제 사용자 참여 0명. 위 항목은 기획·내부 UX 검토 결과이며 사용자 테스트 결과가 아니다.


### 내부 통합 개발 검사

내부 작성 편집기·엔진·질문 전달을 구현한 뒤 기존 검사 포함 75개 자동 검사를 통과했다. 문장 구성 엔진 원본 해시 일치, 입력 범위, 임의 성과 추가 차단, 소수 수치 보존, 작성문에서 실제 근거 질문 생성, 다른 글의 근거 혼합 방지를 검사했다. 추가 읽기 검수에서 첨부 추출과 글 전달 경합, 동일 글 재적용 시 답변 초기화, 저장한 학교 선택 복원 문제를 찾아 수정했다. 실제 사용자 참여 검수는 실시하지 않았다. 아래 운영 검사 기록 이전까지 공개 화면 검사 완료로 해석하지 않는다.


### 내부 통합 공개 운영 검수

Render `dep-dahfilv40ujc73ah1or0`이 커밋 `79f40c2decaf65b8235c970836a1b6eba6b2a476`으로 live 전환한 것을 확인했다. GitHub API 반영 뒤 새 자동 배포 항목이 없어 빌드 캐시를 초기화하는 배포를 요청했다.

- 홈의 상단 '경험·글 작성' → `#prepare/writing` 정상 이동.
- 울산대학교·간호학과를 선택하고 실제 사용자가 아닌 가상 실험 경험으로 1→2→3→4단계 작성. 진행률·남은 입력 표시, 원문 263자 → 문장 정리 272자, 직접 인용 피드백 확인.
- '이 글로 면접 질문 만들기'로 입력 문장에서 6개 질문 생성. 같은 화면에서 답변 139자 입력 → 구체적 인용과 후속 질문 피드백 확인.
- '이 기기에 작성 내용 저장하기' 선택 후 새 검수 프레임에서 학교·학과와 272자 작성문, 4단계 위치 복원 확인. 이전 버튼으로 3단계 이동, 결과 입력란 16px 확인.
- 390px 프레임 문서폭/scrollWidth 375/375, 작성 편집기 295/295. 결과 입력란 포커스 시 후보함 숨김 확인. 320/768/1440px 프레임 문서폭과 scrollWidth도 각각 305/305, 753/753, 1425/1425로 일치했다. 실제 휴대전화 테스트와 구분한다.
- 공개 화면에서 행동 피드백이 '실험을'이라는 명사를 단서로 상황 문장을 인용하는 문제를 발견했다. 행동을 수행한 동사 표현을 기준으로 좁혔고 해당 경험의 실제 비교·제안 문장을 인용하는 회귀 검사를 추가했다.

TXT 내려받기 기능은 구현했지만 이번 공개 브라우저에서 다운로드 파일 내용까지 검사하지 않았다. 기존 회원 프로젝트 동기화와 실제 수험생 참여 검수는 미실시다.

## 2026-09-11 회귀 검수 기록

- 성적 변경: 3.6 → 2.3등급 적용 후 상태영역에 이전값·변경값·등급체계가 표시되고 학교 목록은 합격순위가 아닌 자료 수집 기준을 유지한다. 선택 대학의 학과별 비교 영역도 새 평균을 표시한다.
- 공식 문항: PDF 원문 33건과 문항 위치 색인 180건을 자료 선택 화면에서 확인한다. 문제 요약, 문제 쪽 이동, 해설 후보 쪽 안내, 공식 출처 배지를 확인한다.
- 자체 문항: 100문항의 예시답안·풀이 단계·기준·다음 과제를 확인한다. 자체 문항이 대학 기출 또는 7% 예측으로 표시되지 않는지 검사한다.
- 자기소개서: 문항 목적·경험·행동·근거·지원 연결을 입력한 후 초안을 만들고 자유 편집·인용 보완점·면접 연결을 확인한다. 입력하지 않은 성과·수치·활동이 생성되지 않는지 검사한다.

위 검수는 자동 테스트와 내부 브라우저 점검이다. 실제 수험생·학부모가 참여한 사용자 검수는 별도로 실시하지 않았으며, 공식 PDF 7건 접근 제한과 계정 DB 동기화 미완료를 완료로 표시하지 않는다.

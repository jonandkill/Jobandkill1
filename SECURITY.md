# 보안 및 플랫폼 준수

## 데이터 처리

- 입력 원고는 서버로 전송하지 않습니다.
- 자동 저장 데이터는 현재 브라우저의 `localStorage`에만 남습니다.
- 내보내기는 사용자의 명시적 버튼 조작으로만 실행됩니다.
- 네이버 계정, 비밀번호, 쿠키, 세션 토큰을 요청하거나 저장하지 않습니다.

## 금지된 확장

다음 변경은 이 프로젝트의 안전 경계를 깨므로 병합하지 않습니다.

- 외부 사이트 DOM 자동 조작
- 사람처럼 보이게 만드는 키 입력·마우스 이동·지연 무작위화
- CAPTCHA, 레이트 리밋, 이상행동 탐지 또는 계정 제한 우회
- 사용자의 최종 확인 없는 공개 발행
- 여러 계정을 이용한 대량·반복 게시

연동이 필요한 경우에는 해당 서비스가 현재 제공하는 공식 API와 승인 범위만 사용하고, 공개 발행 전 사용자 확인 단계를 유지합니다.

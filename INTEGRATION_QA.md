# 내부 작성실 통합 검수

## 검수 성격

개발 담당과 별도로 수행하는 코드 검토와 자동 회귀 검사 기록이다. 실제 수험생·학부모 사용자 검사, 면접 전문가의 평가 타당도 검증으로 표현하지 않는다. 운영 화면의 브라우저 확인은 `USER_ACCEPTANCE.md`의 해당 배포 기록과 함께 확인한다.

## 기존 흐름에서 확인한 회귀 위험

- 면접 학교·학과는 답변 저장 함수가 호출될 때 저장 상태에 반영된다. 작성실 이동 시 화면의 현재 선택값을 명시적으로 전달해야 하며, 이전 저장값만 읽으면 방금 선택한 대학·학과를 잃을 수 있다.
- 면접 답변의 식별자는 학교·학과·전형·질문을 포함한다. 다른 대학·학과나 변경된 원문에 이전 답변을 붙이지 않는다.
- 자기소개서 원문과 원문 기반 질문의 답변은 기존 면접 준비실에서 영구 저장하지 않는다. 내부 작성실의 명시적 기기 저장 선택과 혼동하지 않도록 각 화면이 저장 범위를 설명해야 한다.
- 앱은 경로 변경 시 화면을 교체한다. 늦게 완료되는 자료 요청·파일 추출이 다른 화면이나 새 초안을 덮어쓰지 않도록 현재 렌더링 세대를 확인한다.
- 작성문, 학교·학과, 추출 근거 문장은 사용자 입력이다. HTML로 표시할 때 이스케이프하고, 작성문을 URL·API 요청·외부 링크에 자동 포함하지 않는다.

## 실행할 통합 확인

1. 학교·학과 선택 후 답변을 쓰지 않고 작성실에 들어가도 현재 선택이 유지된다.
2. 단계 이동·다른 메뉴 이동·면접 복귀 때 초안이 유지되며, 새 글 시작·글 변경은 기존 글을 묵시적으로 덮어쓰지 않는다.
3. 기기 저장을 선택하지 않으면 저장소에 작성 원문을 남기지 않는다. 선택 후 복원과 선택 해제 후 삭제를 검증한다.
4. 질문 생성은 사용자가 확인한 현재 글에서만 근거 문장을 뽑고, 실제 기출·사실 확인·합격 판단으로 표시하지 않는다.
5. 짧은 글·빈 글·HTML처럼 보이는 글·긴 입력·저장소 오류에 대해 오류 없이 복구할 수 있다.
6. 기존 면접 공통 질문과 자료 가져오기, 질문별 답변 분리, 공식 면접 전형 범위가 유지된다.

## 현재 실행 상태

코드 검토와 신규 통합 검사 6개를 완료했다. 단계 이동 중 편집문 보존, 명시적 질문 전달, 저장 선택·해제, 저장 대학 ID·학과 복원, 변경된 근거 분리, 이벤트 정리를 확인했다. 발견한 답변 소실·PDF 추출 경합·학교 복원·목록에 없는 학과 복원 문제는 수정했다. 이 결과는 이벤트 하니스·코드 검사이며 실제 브라우저·휴대전화 검사와 구분한다.


공개 운영에서는 학교·학과 → 4단계 작성 → 피드백 → 6개 면접 질문 → 답변 피드백을 확인했다. 저장한 작성문·단계·학교·학과가 새 검수 프레임에서 복원됐다. 320/390/768/1440px 문서의 가로 넘침 없음과 입력 중 후보함 숨김을 확인했다. 자세한 관측값은 USER_ACCEPTANCE.md에 기록한다. 실제 사용자 참여와 실기기 시험은 미실시다.

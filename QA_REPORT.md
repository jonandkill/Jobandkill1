# 품질·CS 검증 보고서

## 재현된 원인

기존 `createKeywordDraft`는 주제에 대한 정보를 검색하거나 생성하는 모델이 아니다. 알려진 카테고리별 문장 묶음을 조합한다. 그 밖의 키워드는 일반 글쓰기 절차를 반환한다.

2026-09-10 로컬 재현 결과:

| 입력 | 본문 문자 수(공백 포함) | 기존 점수 | 관찰 |
|---|---:|---:|---|
| 야쿠르트가 몸에 좋은이유 | 1,309 | 75 | 건강·영양 설명 없이 검색 목적/공식 정보/비교 기준 등 일반 절차 |
| 텀블러 세척 | 1,288 | 75 | 세척법 대신 동일한 일반 절차 |
| %%%% | 1,282 | 75 | 비정상 키워드도 본문처럼 포장 |
| 울산면접학원 | 1,744 | 88 | 취업 카테고리 문장 묶음 사용 |

`auditArticle`의 기존 점수는 제목 길이, 분량, 키워드 횟수, 소제목 수, 출처 문자열 존재, 과장 표현 정규식, 문단 길이, 중복 여부의 8개 체크 통과율이다. 주제 관련성이나 출처의 진실성을 검증하지 않는다. 따라서 이 수치를 내용 품질·사실 정확성·검색 노출 가능성으로 해석해서는 안 된다.

## 파일럿의 범위

`tests/pilot-100.test.mjs`는 100개의 합성 시나리오를 이용한 자동 회귀검사이다. 실제 소비자 100명 모집·사용·만족도 조사가 아니며, 실제 사용자 가동률이나 시장 반응을 입증하지 않는다. 외부 API는 mock으로 대체하여 시험 과정에서 사용자 API 비용을 발생시키지 않는다.

## CS 반영 기준

- 키워드가 비정상이면 명확한 입력 오류를 표시하고 API를 호출하지 않는다.
- 주제 설명을 만들 수 없으면 일반 글쓰기 안내를 성공 본문으로 대체하지 않는다.
- API 키 누락, 인증 실패, 호출 제한, 시간 초과는 서로 구별하여 표시한다.
- 빈 응답이나 관계없는 출력은 정상 생성으로 처리하지 않는다.
- 이미지 단계가 실패하더라도 이미 생성된 본문은 보존한다.
- 비용 상한 도달 및 동시 요청의 예산 예약 충돌은 비용 발생 전에 차단한다.
- 건강 관련 내용은 근거와 한계를 구분하며 치료·효능을 보장하지 않는다.

## 결과 기록

2026-09-10 `node --test tests/pilot-100.test.mjs` 실행: 100개 통과, 실패 0개, 건너뜀 0개. 약 1초의 테스트 실행시간은 mock 응답 기반이며 실제 AI 생성 속도가 아니다.

| 시나리오군 | 개수 | 검증 내용 |
|---|---:|---|
| 입력 검증 | 10 | 공백·타입 오류·초과 길이·기호/이모지만 있는 입력 차단 |
| 요청 구성 | 10 | 사용자 메모 데이터 분리, 비저장 요청, 옵션과 비용 예약 |
| 연결 설정 | 10 | API 키 부재 시 호출 없이 명시적 오류 |
| 본문 생성 | 10 | 모의 본문 반환 및 이미지 끈 경우 추가 호출 없음 |
| 이미지 생성 | 10 | 기본 켜짐, 한 장, 제한된 프롬프트 및 저비용 품질 설정 |
| 공급자 오류 | 10 | HTTP 400/401/403/429/500, 네트워크, 타임아웃, JSON 오류, 거절, 미완료 |
| 출력 검증 | 10 | 빈 본문, 부족한 구조, 키워드 불일치, 일반 작성안내 재현 차단 |
| 비용·지속성 | 10 | 입력 상한, 100회 예약 후 재시작해도 차단, 차단시 호출 없음 |
| 동시성 | 10 | 101~110개 동시 예약 중 100개만 허용 |
| 이미지 부분 실패 | 10 | 오류·빈 이미지·잘못된 인코딩 시 본문 보존 및 경고 |

## 발견 사항과 반영

1. 비정상 기호 키워드가 일반 본문으로 반환되는 결함: 신규 입력 검증으로 차단한다.
2. 키워드 미지원 시 일반 작성법을 실제 본문처럼 보여주는 결함: 유료 생성 경로는 일반 템플릿으로 대체하지 않으며, 기존 증상과 동일한 일반 헤딩이 2개 이상인 출력은 `GENERIC_OUTPUT`으로 차단한다.
3. 75점이 내용 품질처럼 보이는 문제: 화면 점수를 형식 점검으로 분리하고 사실·관련성·검색순위 점수가 아님을 고지한다.
4. 이미지 실패 시 본문까지 유실될 위험: 본문은 반환하고 이미지 오류만 별도 안내한다.
5. 비용 중복 호출 위험: 실패의 자동 재시도 없음, 호출 전 영구 예산 예약, 동시 예약 직렬화를 회귀검사한다.

## 출시 판정과 남은 검증

- 합성 회귀검사 100/100은 코드 수준의 해당 조건 통과를 뜻한다. 실제 이용자 만족도·생성 내용 정확도·프로덕션 가동률 100%를 뜻하지 않는다.
- 이미지 mock은 API 데이터 전달 계약만 시험한다. 실제 생성 이미지의 의미·미관·PNG 바이트 유효성·모바일 렌더링을 검증한 것은 아니다.
- 건강 키워드에서 치료 보장 금지와 미검증 자료 취급 지침이 요청에 포함됨을 검사한다. 실제 모델이 생성한 건강 설명의 의학적 사실성은 이 시험에서 검증하지 않았다.
- 제목/도입의 키워드 포함, 길이, 특정 일반 헤딩 차단은 제한된 자동 검사이다. 표현을 바꾼 무관한 글이나 허위 사실을 모두 잡아내는 의미 검증기가 아니다.
- API 키·실서버 설정, 실제 본문/이미지 생성, 모바일 브라우저 동작, 실제 청구액은 별도 운영 검증이 필요하다. 완료되지 않은 운영 검증을 완료로 표시하지 않는다.
- 통합팀 확인: 전체 자동검사 138개 통과. 로컬 브라우저 접속은 `ERR_BLOCKED_BY_CLIENT`로 차단되어 실제 화면 렌더링 검증을 완료하지 못했다.
- 생산성 비교를 위한 기존 작성 소요시간·수정 횟수·새 흐름 소요시간의 실제 사용자 자료는 아직 없다. 비용 예약은 보수적 상한 관리값이며 실제 청구액과 구별해야 한다.
